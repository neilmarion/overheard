<% @posts.each do |post| %>
  <div class="post-outer">
    <div class="post"> 
      <%= render 'posts/post', post: post%>
    </div>
  </div>
<% end %>

<%= render 'shared/pagination', collection: @posts %>
